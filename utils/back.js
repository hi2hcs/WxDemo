// var that = getCurrentPages()[getCurrentPages().length - 1];//获取当前page实例

var back=function () {
  wx.showToast({
    title: '返回....',
    icon: none
  })
}

module.exports = {
  back: back
}